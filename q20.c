#include <stdio.h>

int main() {
    int oneDigit = 7;     // Example one-digit number
    int twoDigit = 42;    // Example two-digit number
    int sum = oneDigit + twoDigit;

    printf("Sum = %d\n", sum);
    return 0;
}